# BULL Trading - Сайт для найму трейдерів

Сучасний односторінковий веб-сайт для найму та рекрутингу трейдерів з адмін панеллю для управління контентом.

![BULL Trading](https://img.shields.io/badge/BULL-Trading-green)
![React](https://img.shields.io/badge/React-18.3-blue)
![TypeScript](https://img.shields.io/badge/TypeScript-5.8-blue)
![MongoDB](https://img.shields.io/badge/MongoDB-Atlas-green)

---

## 📋 Зміст

- [Особливості](#-особливості)
- [Технології](#-технології)
- [Вимоги до системи](#-вимоги-до-системи)
- [Повне налаштування з нуля](#-повне-налаштування-з-нуля)
  - [Крок 1: Встановлення Node.js](#крок-1-встановлення-nodejs)
  - [Крок 2: Клонування репозиторію](#крок-2-клонування-репозиторію)
  - [Крок 3: Встановлення залежностей](#крок-3-встановлення-залежностей)
  - [Крок 4: Налаштування MongoDB](#крок-4-налаштування-mongodb)
  - [Крок 5: Налаштування змінних середовища](#крок-5-налаштування-змінних-середовища)
  - [Крок 6: Ініціалізація бази даних](#крок-6-ініціалізація-бази-даних)
  - [Крок 7: Запуск проекту](#крок-7-запуск-проекту)
  - [Крок 8: Налаштування Telegram (опціонально)](#крок-8-налаштування-telegram-опціонально)
- [Адмін панель](#-адмін-панель)
- [Деплой на production](#-деплой-на-production)
- [Troubleshooting](#-troubleshooting)
- [Додаткова документація](#-додаткова-документація)

---

## 🎯 Особливості

### Фронтенд
- ⚡ **Vite + React** - швидка розробка та build
- 🎨 **Tailwind CSS** - сучасний дизайн
- 🧩 **shadcn/ui** - високоякісні UI компоненти
- ✨ **Анімації** - плавні SVG анімації на фоні
- 📱 **Responsive** - адаптивний дизайн для всіх пристроїв
- 🎭 **Dark theme** - темна тема з зеленими акцентами
- ✅ **Валідація форм** - повна валідація телефону та email в реальному часі

### Backend
- 🚀 **Express.js** - швидкий Node.js сервер
- 🗄️ **MongoDB** - NoSQL база даних
- 🔒 **JWT** - безпечна автентифікація
- 📊 **RESTful API** - чистий API для всіх операцій
- ✅ **Валідація даних** - багаторівнева валідація (frontend + backend + database)
- 🛡️ **Захист від спаму** - rate limiting, honeypot, IP tracking

### Адмін Панель
- 📩 **Управління заявками** - перегляд та обробка заявок з сайту
- 📤 **Telegram інтеграція** - автоматична відправка заявок в Telegram
- 👥 **Управління командою** - додавання/редагування членів команди
- 📸 **Завантаження фото** - підтримка фото або автоматичні ініціали
- 🎯 **Переваги** - керування перевагами компанії
- 📈 **Статистика** - редагування статистичних даних
- 📝 **Контент** - зміна всіх текстів на сайті
- 🔐 **Захищений доступ** - JWT автентифікація

---

## 🛠 Технології

### Frontend
- **React 18** - UI бібліотека
- **TypeScript** - типізація
- **Vite** - build tool
- **Tailwind CSS** - utility-first CSS
- **shadcn/ui** - компоненти
- **React Router** - маршрутизація
- **Tanstack Query** - управління даними

### Backend
- **Node.js** - runtime
- **Express.js** - web framework
- **MongoDB** - база даних
- **Mongoose** - ODM
- **JWT** - автентифікація
- **bcryptjs** - хешування паролів
- **CORS** - cross-origin запити

### DevOps
- **Nodemon** - автоматичний перезапуск
- **Concurrently** - паралельний запуск
- **ESLint** - лінтинг
- **PostCSS** - обробка CSS

---

## 💻 Вимоги до системи

Перед початком встановлення переконайтеся, що у вас встановлено:

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0 (або **yarn**, **pnpm**, **bun**)
- **MongoDB** >= 6.0 (локально) або доступ до **MongoDB Atlas** (хмарна база)
- **Git** (для клонування репозиторію)

### Перевірка версій

```bash
node --version   # Має бути v18.0.0 або вище
npm --version    # Має бути 9.0.0 або вище
git --version    # Будь-яка версія
```

---

## 🚀 Повне налаштування з нуля

### Крок 1: Встановлення Node.js

#### Windows:
1. Завантажте Node.js з [офіційного сайту](https://nodejs.org/)
2. Виберіть LTS версію (рекомендовано)
3. Встановіть з налаштуваннями за замовчуванням
4. Перезапустіть термінал

#### macOS:
```bash
# Через Homebrew
brew install node

# Або завантажте з офіційного сайту
```

#### Linux (Ubuntu/Debian):
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### Крок 2: Клонування репозиторію

```bash
# Клонуйте репозиторій
git clone https://github.com/your-repo/bulltrading-ukraine-hiring.git

# Перейдіть в директорію проекту
cd bulltrading-ukraine-hiring
```

Якщо у вас немає доступу до репозиторію, просто розпакуйте архів проекту.

### Крок 3: Встановлення залежностей

```bash
# Встановіть всі необхідні пакети
npm install
```

Це встановить всі залежності для фронтенду та бекенду. Процес може зайняти 2-5 хвилин.

### Крок 4: Налаштування MongoDB

У вас є два варіанти: локальна MongoDB або хмарна MongoDB Atlas.

#### Варіант A: Локальна MongoDB (для розробки)

**Windows:**
1. Завантажте MongoDB Community Server з [офіційного сайту](https://www.mongodb.com/try/download/community)
2. Встановіть з налаштуваннями за замовчуванням
3. MongoDB автоматично запуститься як служба Windows

**macOS:**
```bash
brew tap mongodb/brew
brew install mongodb-community
brew services start mongodb-community
```

**Linux:**
```bash
sudo apt-get install mongodb
sudo systemctl start mongodb
sudo systemctl enable mongodb  # Автозапуск
```

**Перевірка:**
```bash
# Перевірте чи MongoDB запущена
mongosh  # Або mongo для старих версій
```

#### Варіант B: MongoDB Atlas (рекомендовано для production)

1. **Створіть акаунт:**
   - Перейдіть на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
   - Зареєструйтесь або увійдіть

2. **Створіть кластер:**
   - Натисніть **"Build a Database"**
   - Виберіть **FREE tier (M0)**
   - Виберіть регіон (наприклад, Frankfurt для Європи)
   - Назвіть кластер: `bulltrading-cluster`
   - Натисніть **"Create"**

3. **Створіть користувача бази даних:**
   - Перейдіть до **Database Access** (ліва панель)
   - Натисніть **"Add New Database User"**
   - Username: `bulltrading_user`
   - Password: згенеруйте сильний пароль (збережіть його!)
   - Database User Privileges: **Read and write to any database**
   - Натисніть **Add User**

4. **Налаштуйте Network Access:**
   - Перейдіть до **Network Access** (ліва панель)
   - Натисніть **"Add IP Address"**
   - Натисніть **"Allow Access from Anywhere"** (0.0.0.0/0)
   - Натисніть **Confirm**

5. **Отримайте Connection String:**
   - Поверніться до **Database** → **Connect**
   - Виберіть **"Connect your application"**
   - Driver: **Node.js**, Version: **5.5 or later**
   - Скопіюйте connection string:
   ```
   mongodb+srv://bulltrading_user:<password>@bulltrading-cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
   - Замініть `<password>` на ваш реальний пароль
   - Додайте назву бази після `.net/`:
   ```
   mongodb+srv://bulltrading_user:ВАШ_ПАРОЛЬ@bulltrading-cluster.xxxxx.mongodb.net/bulltrading?retryWrites=true&w=majority
   ```

### Крок 5: Налаштування змінних середовища

Створіть файл `.env` в корені проекту (поряд з `package.json`):

```env
# MongoDB Connection
# Для локальної MongoDB:
MONGODB_URI=mongodb://localhost:27017/bulltrading

# Або для MongoDB Atlas (замініть на ваш connection string):
# MONGODB_URI=mongodb+srv://bulltrading_user:ВАШ_ПАРОЛЬ@cluster.xxxxx.mongodb.net/bulltrading?retryWrites=true&w=majority

# Server Configuration
PORT=5000

# JWT Secret (генеруйте складний випадковий рядок, мінімум 32 символи)
# Для генерації виконайте: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_min_32_chars

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Telegram Bot (опціонально - дивіться Крок 8)
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token
# TELEGRAM_CHAT_ID=your_telegram_chat_id
```

**⚠️ ВАЖЛИВО:**
- Ніколи не комітьте файл `.env` в Git!
- Для production змініть `JWT_SECRET` на складний випадковий рядок
- Змініть `ADMIN_PASSWORD` на сильний пароль

**Генерація JWT_SECRET:**
```bash
# В терміналі виконайте:
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопіюйте згенерований рядок в `.env` як `JWT_SECRET`.

### Крок 6: Ініціалізація бази даних

Після налаштування MongoDB та `.env` файлу, ініціалізуйте базу даних початковими даними:

```bash
npm run seed
```

Ця команда:
- Підключиться до MongoDB
- Очистить існуючі дані (якщо є)
- Створить початкові дані:
  - 4 члени команди
  - 6 переваг компанії
  - 3 статистичні показники
  - Контент для всіх секцій сайту

Після успішного виконання ви побачите:
```
✅ MongoDB connected
🗑️  Cleared existing data
✅ Team members seeded
✅ Advantages seeded
✅ Stats seeded
✅ Content seeded
✨ Database seeded successfully!
```

### Крок 7: Запуск проекту

Тепер можна запустити проект. У вас є кілька варіантів:

#### Варіант 1: Запуск всього разом (рекомендовано)

```bash
npm run dev:all
```

Це запустить:
- **Frontend** на http://localhost:8080
- **Backend** на http://localhost:5000

#### Варіант 2: Окремий запуск

```bash
# Термінал 1 - Backend
npm run server

# Термінал 2 - Frontend
npm run dev
```

#### Перевірка роботи

1. **Відкрийте браузер:** http://localhost:8080
2. Ви повинні побачити головну сторінку сайту
3. Перевірте всі секції: Hero, Про компанію, Переваги, Команда, Контакти

### Крок 8: Налаштування Telegram (опціонально)

Telegram інтеграція дозволяє автоматично отримувати заявки з сайту прямо в Telegram.

#### Швидке налаштування:

1. **Створіть Telegram бота:**
   - Відкрийте [@BotFather](https://t.me/BotFather) в Telegram
   - Надішліть `/newbot`
   - Введіть ім'я: `BULL Trading Applications`
   - Введіть username: `bulltrading_app_bot` (має закінчуватись на `bot`)
   - Скопіюйте токен: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz...`

2. **Отримайте Chat ID:**
   - Відкрийте [@userinfobot](https://t.me/userinfobot)
   - Натисніть **Start**
   - Скопіюйте ваш Chat ID: `123456789`

3. **Додайте в `.env`:**
   ```env
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz...
   TELEGRAM_CHAT_ID=123456789
   ```

4. **Перезапустіть сервер:**
   ```bash
   # Зупиніть сервер (Ctrl+C) і запустіть знову
   npm run dev:all
   ```

5. **Протестуйте:**
   - Заповніть форму на сайті
   - Перевірте Telegram - має прийти повідомлення

**Детальна інструкція:** [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md)

---

## 🎨 Адмін Панель

### Доступ до адмін панелі

1. Відкрийте: http://localhost:8080/admin/login
2. Використайте дані для входу:
   - **Логін:** `admin` (або значення з `ADMIN_USERNAME` в `.env`)
   - **Пароль:** `admin123` (або значення з `ADMIN_PASSWORD` в `.env`)

### Розділи адмін панелі

#### 1. Dashboard (`/admin/dashboard`)
- Огляд всіх розділів
- Швидкий доступ до управління

#### 2. Заявки (`/admin/applications`)
- Перегляд всіх заявок з сайту
- Статуси: Нова, В обробці, Завершена, Відхилена
- Telegram статус (відправлено/помилка)
- Повторна відправка в Telegram
- Додавання приміток
- Видалення заявок

#### 3. Команда (`/admin/team`)
- Додавання нових членів
- Завантаження фото (до 2MB)
- Автоматичні ініціали з імені
- Редагування та видалення
- Зміна порядку відображення

#### 4. Переваги (`/admin/advantages`)
- Управління перевагами компанії
- Вибір іконок (6 варіантів)
- Вибір кольорів
- Зміна порядку

#### 5. Статистика (`/admin/stats`)
- Редагування цифр
- Зміна підписів
- Управління порядком

#### 6. Контент (`/admin/content`)
- Hero блок (заголовок, підзаголовок, кнопка)
- Про компанію (заголовки, параграфи)
- Переваги (заголовок, підзаголовок)
- Команда (заголовок, підзаголовок)
- Контакти (заголовок, підзаголовок)
- Footer (опис, контакти, соцмережі)

---

## 🚢 Деплой на production

### Швидкий деплой на Render (рекомендовано)

Render дозволяє задеплоїти весь проект (frontend + backend) в одному Web Service.

#### Підготовка:

1. **Створіть MongoDB Atlas** (якщо ще не створили - дивіться Крок 4, Варіант B)
2. **Підготуйте GitHub репозиторій:**
   ```bash
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

#### Деплой:

1. **Створіть Web Service на Render:**
   - Зайдіть на [Render.com](https://render.com)
   - Натисніть **New +** → **Web Service**
   - Підключіть ваш GitHub репозиторій
   - Виберіть репозиторій проекту

2. **Налаштування:**
   - **Name:** `bulltrading` (або будь-яка назва)
   - **Region:** `Frankfurt (EU Central)`
   - **Branch:** `main`
   - **Runtime:** `Node`
   - **Build Command:** `npm run render:build`
   - **Start Command:** `npm start`
   - **Instance Type:** `Free`

3. **Environment Variables:**
   Натисніть **Advanced** → **Add Environment Variable**:
   
   | Key | Value |
   |-----|-------|
   | `MONGODB_URI` | ваш connection string з Atlas |
   | `JWT_SECRET` | складний випадковий рядок (32+ символів) |
   | `ADMIN_USERNAME` | ваш логін адміна |
   | `ADMIN_PASSWORD` | ваш сильний пароль |
   | `NODE_ENV` | `production` |
   | `PORT` | `10000` |
   | `TELEGRAM_BOT_TOKEN` | (опціонально) |
   | `TELEGRAM_CHAT_ID` | (опціонально) |

4. **Deploy:**
   - Натисніть **Create Web Service**
   - Render почне build (5-10 хвилин)
   - Очікуйте зелений статус ✅

5. **Перевірка:**
   - Відкрийте ваш URL: `https://your-service-name.onrender.com`
   - Перевірте всі сторінки
   - Адмін панель: `https://your-service-name.onrender.com/admin/login`

**Детальна інструкція:** [RENDER_DEPLOY.md](./RENDER_DEPLOY.md)

### Альтернативні варіанти деплою

- **Vercel (frontend) + Railway (backend)** - дивіться [DEPLOYMENT.md](./DEPLOYMENT.md)
- **Netlify (frontend) + Heroku (backend)**
- **DigitalOcean App Platform**
- **AWS / Google Cloud / Azure**

---

## 🐛 Troubleshooting

### Проблема: MongoDB connection error

**Помилка:** `MongoServerError: connection timed out`

**Рішення:**
1. Перевірте чи MongoDB запущена (локально):
   ```bash
   # Windows
   services.msc  # Знайдіть MongoDB service

   # macOS/Linux
   brew services list  # або systemctl status mongodb
   ```

2. Перевірте connection string в `.env`
3. Для Atlas - перевірте Network Access (має бути 0.0.0.0/0)
4. Перевірте пароль в connection string

---

### Проблема: Port already in use

**Помилка:** `Error: listen EADDRINUSE: address already in use :::5000`

**Рішення:**
1. Знайдіть процес що використовує порт:
   ```bash
   # Windows
   netstat -ano | findstr :5000

   # macOS/Linux
   lsof -i :5000
   ```

2. Зупиніть процес або змініть PORT в `.env`

---

### Проблема: npm install fails

**Помилка:** `npm ERR! code ERESOLVE`

**Рішення:**
```bash
# Очистіть кеш
npm cache clean --force

# Видаліть node_modules та package-lock.json
rm -rf node_modules package-lock.json

# Встановіть знову
npm install
```

---

### Проблема: Seed не працює

**Помилка:** `Error seeding database`

**Рішення:**
1. Перевірте MongoDB connection
2. Перевірте `.env` файл
3. Переконайтеся що MongoDB запущена
4. Спробуйте підключитись вручну:
   ```bash
   mongosh mongodb://localhost:27017/bulltrading
   ```

---

### Проблема: Frontend не може підключитись до API

**Помилка:** `Failed to fetch` або `CORS error`

**Рішення:**
1. Перевірте чи backend запущений (`npm run server`)
2. Перевірте порт backend (має бути 5000)
3. Перевірте proxy в `vite.config.ts`
4. Перезапустіть обидва сервери

---

### Проблема: Telegram не працює

**Помилка:** Заявки не приходять в Telegram

**Рішення:**
1. Перевірте `TELEGRAM_BOT_TOKEN` та `TELEGRAM_CHAT_ID` в `.env`
2. Переконайтеся що натиснули **Start** у боті
3. Перевірте логи сервера на помилки
4. Дивіться детальну інструкцію: [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md)

---

### Проблема: Адмін панель не відкривається

**Помилка:** 404 або білий екран

**Рішення:**
1. Перевірте URL: `http://localhost:8080/admin/login`
2. Перевірте чи frontend запущений
3. Очистіть кеш браузера (Ctrl+Shift+R)
4. Перевірте консоль браузера на помилки (F12)

---

## 📚 Додаткова документація

- [RENDER_DEPLOY.md](./RENDER_DEPLOY.md) - **Швидкий деплой на Render (один сервіс)**
- [DEPLOYMENT.md](./DEPLOYMENT.md) - Повна інструкція з деплою (frontend + backend окремо)
- [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md) - Детальне налаштування Telegram бота
- [NAMECHEAP_DEPLOY.md](./NAMECHEAP_DEPLOY.md) - Купівля домену та деплой на Namecheap
- [SECURITY.md](./SECURITY.md) - Захист від спаму, ботів та атак
- [VALIDATION.md](./VALIDATION.md) - Детальна документація про валідацію форм
- [FEATURES.md](./FEATURES.md) - Повний список можливостей проекту
- [README_ADMIN.md](./README_ADMIN.md) - Документація адмін панелі

---

## 📝 Скрипти

```bash
# Розробка
npm run dev          # Запустити тільки фронтенд (Vite, порт 8080)
npm run server       # Запустити тільки backend (Express, порт 5000)
npm run dev:all      # Запустити фронтенд + backend разом

# База даних
npm run seed         # Ініціалізувати базу даних початковими даними

# Production
npm run build        # Збудувати фронтенд для production
npm start            # Запустити production сервер
npm run render:build # Build для Render (install + build + seed)

# Інше
npm run lint         # Запустити ESLint
npm run preview      # Preview production build
```

---

## 🔒 Безпека

⚠️ **ВАЖЛИВО для production:**

1. ✅ Змініть `JWT_SECRET` на складний випадковий рядок (32+ символів)
2. ✅ Змініть `ADMIN_PASSWORD` на сильний пароль
3. ✅ Використовуйте HTTPS
4. ✅ Налаштуйте CORS для конкретних доменів
5. ✅ Rate limiting вже налаштований
6. ✅ Паролі хешуються через bcryptjs
7. ✅ Регулярно оновлюйте залежності (`npm audit`)

---

## 📁 Структура проекту

```
bulltrading-ukraine-hiring/
├── src/                          # Frontend код
│   ├── components/              # React компоненти
│   │   ├── AboutSection.tsx
│   │   ├── AdvantagesSection.tsx
│   │   ├── TeamSection.tsx
│   │   ├── ContactSection.tsx
│   │   ├── HeroSection.tsx
│   │   ├── Navigation.tsx
│   │   ├── Footer.tsx
│   │   └── ui/                  # shadcn/ui компоненти
│   ├── pages/                   # Сторінки
│   │   ├── Index.tsx
│   │   └── admin/               # Адмін панель
│   │       ├── Login.tsx
│   │       ├── Dashboard.tsx
│   │       ├── ApplicationsManager.tsx
│   │       └── SectionManager.tsx
│   ├── lib/                     # Утиліти
│   │   ├── api.ts              # API клієнт
│   │   └── utils.ts
│   ├── hooks/                   # Custom hooks
│   └── assets/                  # Статичні файли
│
├── server/                      # Backend код
│   ├── index.js                # Express сервер
│   ├── seed.js                 # Скрипт ініціалізації БД
│   ├── models/                 # Mongoose моделі
│   │   ├── TeamMember.js
│   │   ├── Advantage.js
│   │   ├── Stats.js
│   │   ├── Content.js
│   │   └── Application.js
│   ├── routes/                 # API маршрути
│   │   ├── auth.js
│   │   ├── team.js
│   │   ├── advantages.js
│   │   ├── stats.js
│   │   ├── content.js
│   │   └── applications.js
│   └── middleware/             # Middleware
│       └── auth.js
│
├── public/                      # Публічні файли
├── dist/                        # Зібраний фронтенд (після build)
├── .env                        # Змінні середовища (не комітиться)
├── package.json
├── vite.config.ts
├── tailwind.config.ts
└── README.md
```

---

## 🔌 API Endpoints

### Автентифікація
```
POST   /api/auth/login          # Вхід в систему
POST   /api/auth/verify         # Перевірка токену
```

### Заявки
```
GET    /api/applications        # Отримати всі заявки (auth)
GET    /api/applications/:id    # Отримати одну заявку (auth)
POST   /api/applications        # Створити заявку (public)
PUT    /api/applications/:id    # Оновити заявку (auth)
DELETE /api/applications/:id    # Видалити заявку (auth)
POST   /api/applications/:id/resend # Повторно відправити в Telegram (auth)
```

### Команда
```
GET    /api/team                # Отримати всіх членів
GET    /api/team/:id            # Отримати одного члена
POST   /api/team                # Створити члена (auth)
PUT    /api/team/:id            # Оновити члена (auth)
DELETE /api/team/:id            # Видалити члена (auth)
```

### Переваги
```
GET    /api/advantages          # Отримати всі переваги
POST   /api/advantages          # Створити перевагу (auth)
PUT    /api/advantages/:id      # Оновити перевагу (auth)
DELETE /api/advantages/:id      # Видалити перевагу (auth)
```

### Статистика
```
GET    /api/stats               # Отримати статистику
POST   /api/stats               # Створити статистику (auth)
PUT    /api/stats/:id           # Оновити статистику (auth)
DELETE /api/stats/:id           # Видалити статистику (auth)
```

### Контент
```
GET    /api/content             # Отримати весь контент
GET    /api/content/:section    # Отримати контент секції
POST   /api/content/:section    # Оновити контент (auth)
```

---

## 📄 Ліцензія

Цей проект ліцензовано під MIT License.

---

## 👨‍💻 Автор

**kun3741**

- Website: https://vaysed.me

---

## 🙏 Подяки

- [React](https://reactjs.org/)
- [Vite](https://vitejs.dev/)
- [Tailwind CSS](https://tailwindcss.com/)
- [shadcn/ui](https://ui.shadcn.com/)
- [MongoDB](https://www.mongodb.com/)
- [Express](https://expressjs.com/)

---

**Зроблено з ❤️, by kun3741**
